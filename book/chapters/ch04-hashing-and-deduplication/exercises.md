# 練習題

## 基礎題

1. 若只用 `email` 當成去重鍵，哪些情況可能誤判為不同人？
2. 若同一個人可能同時使用公司信箱與私人信箱，去重策略要如何調整？
3. 為什麼去重時通常要保留原始輸入順序？

## 實作題

1. 修改 `deduplicate_records`，讓它可以回傳每筆保留資料對應的原始索引。
2. 增加一個選項，允許只忽略字串前後空白、不轉小寫。
3. 將重複資料輸出成另一份清單，供人工覆核。