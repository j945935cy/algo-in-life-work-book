# 前言

這本書的目標是將「日常與職場中的問題」轉化成可執行的演算法實作，並透過可重現的資料、測試與出版流程，讓讀者從思路到成品都能完整掌握。

本書採用以下三個核心原則：

1. 可執行：每個章節都應該有可直接運行的範例程式碼。
2. 可測試：每個章節應附上單元測試，以確保邏輯與輸出一致。
3. 可出版：內容與資產應該以 Jupyter Book / Pandoc 的方式輸出成 PDF 與 EPUB。

這份專案同時也是一個「作者與 Codex 協作」的範例專案，用 Git checkpoints 控制版本，讓寫作、程式碼與測試三者能夠同步前進。
